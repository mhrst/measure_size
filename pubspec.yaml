name: measure_size
description: A widget that provides a callback
homepage: https://github.com/mhrst/measure_size
repository: https://github.com/mhrst/measure_size
issue_tracker: https://github.com/mhrst/measure_size/issues
version: 2.0.0

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  pedantic: ^1.11.0
  flutter_test:
    sdk: flutter
